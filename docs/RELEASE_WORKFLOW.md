<h1 align="center">GitNapse Release Workflow</h1>

<div id="content"></div>
<h2 align="center">Contents</h2>
<ul>
  <li><a href="#overview">Overview</a></li>
  <li><a href="#triggers">Workflow Triggers</a></li>
  <li><a href="#artifacts">Build Artifacts</a></li>
  <li><a href="#commands">Versioning Commands</a></li>
  <li><a href="#manual-run">Manual Release Run</a></li>
  <li><a href="#collaboration">Collaboration Policy</a></li>
  <li><a href="#notes">Official Notes</a></li>
</ul>

<h2 id="overview" align="center">Overview</h2>
<p>
  GitNapse uses <code>.github/workflows/release.yml</code> to compile release binaries and publish them as GitHub Release assets.
  The pipeline builds platform artifacts for Windows, Linux (Ubuntu, Arch, Fedora), and macOS, then uploads them to the release tag.
</p>

<h2 id="triggers" align="center">Workflow Triggers</h2>
<ul>
  <li><code>push</code> on tags that match <code>v*</code> (example: <code>v1.0.0</code>)</li>
  <li><code>workflow_dispatch</code> with an existing <code>release_tag</code> input</li>
</ul>

<h2 id="artifacts" align="center">Build Artifacts</h2>
<ul>
  <li><code>gitnapse-&lt;tag&gt;-linux-ubuntu-x86_64.tar.gz</code></li>
  <li><code>gitnapse-&lt;tag&gt;-linux-arch-x86_64.tar.gz</code></li>
  <li><code>gitnapse-&lt;tag&gt;-linux-fedora-x86_64.tar.gz</code></li>
  <li><code>gitnapse-&lt;tag&gt;-windows-x86_64.zip</code></li>
  <li><code>gitnapse-&lt;tag&gt;-macos-&lt;arch&gt;.tar.gz</code></li>
</ul>

<h2 id="commands" align="center">Versioning Commands</h2>
<p>Create and publish a new version tag:</p>
<pre><code class="language-bash">git checkout main
git pull --ff-only
git tag -a v1.0.0 -m "GitNapse v1.0.0"
git push origin v1.0.0
</code></pre>
<p>
  After the tag push, GitHub Actions runs the release workflow and publishes assets in the corresponding GitHub Release.
</p>

<h2 id="manual-run" align="center">Manual Release Run</h2>
<ol>
  <li>Open <strong>GitHub -&gt; Actions -&gt; Release</strong>.</li>
  <li>Click <strong>Run workflow</strong>.</li>
  <li>Set <code>release_tag</code> to an existing tag (example: <code>v1.0.0</code>).</li>
  <li>Run the workflow to rebuild and upload assets with <code>--clobber</code> behavior.</li>
</ol>

<h2 id="collaboration" align="center">Collaboration Policy</h2>
<p>
  For protected-branch collaboration flow (<code>main</code> via Pull Requests only), see
  <a href="./COLLABORATIVE_SECTION.md">COLLABORATIVE_SECTION.md</a>.
</p>

<h2 id="notes" align="center">Official Notes</h2>
<ul>
  <li>The workflow uses <code>GITHUB_TOKEN</code> with least privilege, elevating <code>contents: write</code> only in the publish job.</li>
  <li>Release notes are generated by GitHub using <code>gh release create --generate-notes</code>.</li>
  <li>Artifact passing between jobs uses <code>actions/upload-artifact</code> and <code>actions/download-artifact</code>.</li>
</ul>
